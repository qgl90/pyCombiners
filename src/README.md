# src Folder

This is the Python source root for the package.

## Contents

- `trackcomb/`
  - Main package implementing data models, physics math, combination logic,
    input/output handling, and CLI entrypoint.

## How It Interacts With Other Folders

- Installed/loaded via `pyproject.toml` at repository root.
- Called by scripts in `examples/`.
- Verified by tests in `tests/`.
